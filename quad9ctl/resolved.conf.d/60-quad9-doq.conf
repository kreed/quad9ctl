# Route public DNS through the local Quad9 DoQ proxy.  More-specific per-link
# domains from NetworkManager, such as "lan" or VPN domains, still take priority.
[Resolve]
DNS=127.0.0.1:53
Domains=~.
FallbackDNS=
